<div class="row start">
{%- for entry in collections.all | eleventyNavigation %}<div class="col-md-4 col-4 p-3 p-md-5 text-center"><a href="{{ entry.url }}" class="text-white">{{ entry.title }}</a></div>{%- endfor %} 
  <div class="col-md-10 offset-md-1 p-3 p-md-5 mt-5 mb-5 text-center">
  <div class="p-3 p-md-5 mt-5 mb-5 ">
  <h1 class="mt-5 mb-5"><strong><a href="{{metadata.url}}" class="text-white">{{ metadata.title }}</a></strong></h1>
  <h2 class="display-4 mt-3 mb-5"><strong>{{ metadata.description }}</strong></h2>
  </div>
  </div>
  </div>